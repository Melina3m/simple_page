name: Deploy to Railway

on:
  push:
    branches: 
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Check out repository code
      uses: actions/checkout@v2

    - name: Set up Python 3.10
      uses: actions/setup-python@v2
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m venv venv
        venv/bin/pip install -r requirements.txt

    # Agregar un paso de depuración
    - name: Debug the environment
      run: |
        echo "Python version: $(python --version)"
        echo "Installed packages: $(pip freeze)"
        echo "NPM version: $(npm --version)"
        echo "Railway CLI version: $(railway --version)"

    # Intentar instalar Railway CLI via npm
    - name: Install Railway CLI via npm
      run: |
        sudo apt-get install npm
        sudo npm install -g railway

    - name: Verify Railway CLI installation
      run: railway --version

    - name: Deploy to Railway
      run: |
        railway login --token ${{ secrets.RAILWAY_TOKEN }}
        railway up
